import React from "react";

interface Props {
  artPiece: any;
}
const VerificationRejected: React.FC<Props> = ({ artPiece }) => {
  return <div>VerificationRejected</div>;
};

export default VerificationRejected;
